build:
  docker:
    web: Dockerfile
  config:
    MIX_ENV: prod
    GITHUB_SECRET: $GITHUB_SECRET
    LIVE_VIEW_SALT:  $LIVE_VIEW_SALT
    SECRET_KEY_BASE: $SECRET_KEY_BASE
    REDIS_URL: $REDIS_URL
